[package]
name = "anya-scoring"
version = "0.1.0"
edition = "2024"
publish = false
description = "Internal scoring engine for Anya (private)"


[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
